"""Слой репозиториев: единый Unit-of-Work и append-only Event Store.

Это контракт, на котором Фаза 2+ строит ВСЕ доменные сервисы:
- ``uow.mutate_with_event`` — единственный путь мутации; атомарно со-записывает
  событие в той же транзакции (D-04, EVT-01, NFR-06);
- ``event_repository.append_event`` — узкий append-only INSERT в events (NFR-06/
  EVT-03): записать событие можно, изменить/удалить — нельзя по дизайну кода
  (в дополнение к БД-триггеру + REVOKE из плана 03).
"""
